   
    <% @promotions.each do |promotion| %>
   
    <div class="row">
        <div class="col-12 col-md-7 col-lg-6 mb-3">
            <div class="card">
                <div class="card-body">
                    <p><%= promotion.descripition %></p>
                    <p><%= promotion.name %></p>
                    <p><%= promotion.code %></p>
                </div>
            </div>
        </div>
    </div>
    <% end %>